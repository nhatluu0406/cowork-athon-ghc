schema_version: "1.0"
extension:
  id: lean-implement
  name: "Lean Implement"
  version: "1.0.0"
  description: "Token-efficient task implementation using context-assembler. Implements ONE task at a time, updates change-log, enforces 500-line doc limit."

commands:
  speckit.lean-implement:
    description: "Implement a single task using scoped context. Args: TASK-ID"
    user_invocable: true

hooks: {}
