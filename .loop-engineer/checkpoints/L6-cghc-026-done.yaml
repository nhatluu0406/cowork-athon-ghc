checkpoint: L6-cghc-026-done
created_at: "2026-07-11T22:10:00Z"
run_id: RUN-0007
loop_status: RUNNING
gate: null
resumable: true
tasks_done_total: 27
just_finished:
  - "CGHC-026 (runtime extensions) DONE — 27/28. Seam-based service/src/extensions: skill registry (RE1), MCP add/enable/disable/remove lifecycle (RE2, SSRF-validated endpoint), Cowork-GHC workflow templates (RE4), and RE5 failure isolation (a broken extension is quarantined with a redacted diagnostic and NO exception escapes any registry method)."
review: "Independent code review (code-reviewer != implementer): CHANGES_REQUIRED (1 HIGH + 1 MEDIUM + 2 LOW), ALL FIXED + re-verified. HIGH: sticky quarantine (disable no longer clears failed; explicit clearQuarantine is the one skill un-quarantine route). MEDIUM: remove is best-effort (a rejecting live disconnect still drops the entry). LOW: hardened runIsolated catch body; LOW: template store under isolation. RE5 crash-safety verified intact."
health:
  full_suite: "530 pass / 0 fail / 0 skip (stable over 2 runs)"
  tsc: "clean"
  extensions_tests: "30 pass"
  unresolved_critical: 0
  unresolved_high: 0
tier2_carry_forward:
  - "Live skill execution via OpenCode (real SkillRunner); live MCP process/connection (real McpAdapter); extensions HTTP router + UI; surface plugin.added/integration.updated frames as EV."
remaining:
  - "CGHC-028 (packaged E2E release verification) — the ONLY task left; the L9 release-verifier gate. Folds every accumulated Tier 2 live leg."
cghc_028_plan:
  wave_a: "Tier 2 LIVE supervisor: spawn the composed loopback service + OpenCode child (reuse buildLaunchSpec + tools/capture-frames launch.ts/opencode-http.ts), inject credentials from the keyring at launch, and implement the four live adapters that fill the composition injection seams (RuntimeHealth, SessionStore create/list/get/rename/replay, RuntimeReplyPort, ProviderConnector) — one owner per child lifecycle, PID/port under .runtime/."
  wave_b: "Electron packaging: make electron-builder real (single-bundle sandboxed preload, exclude renderer sourcemaps, asarUnpack @napi-rs/keyring); wire the four .bat (init/start/stop/clean) to the real app lifecycle via the neutral CLI with honest exit codes + root-independence."
  wave_c: "Packaged E2E verification (release-verifier): the critical path on the PACKAGED build (init->start->workspace->provider/model->session->streaming->permission->file-on-disk->stop->resume->clean) + the PR7 provider-error leg + template re-run + session resume; bounded DeepSeek live legs (<=3 successful/task, keyring credential, per the standing authorization). Report PASS/PARTIAL/FAIL honestly — NEVER dev-server as release evidence."
resume_hint: >
  27/28 DONE, all reviewed 0 Crit/High, suite 530 pass, tsc clean. Only CGHC-028 (L9 packaged release
  verification) remains — run it in three waves (live supervisor -> packaging + .bat wiring -> packaged
  E2E with bounded DeepSeek legs). The composition injection seams + capture-frames launch/http code are
  the building blocks. Web DEFERRED (ADR 0007). No credential gate remains.
