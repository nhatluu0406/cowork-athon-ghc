checkpoint: L6-wave-3b2
created_at: "2026-07-11T09:30:00Z"
run_id: RUN-0007
loop_status: RUNNING
gate: WAVE_PASS
resumable: true
note: "Wave 3 batch B-2. CGHC-006 (lifecycle scripts) + CGHC-013 (session orchestration). Both independently reviewed (reviewer != owner); no owner stalls this batch. CGHC-013 review returned two MEDIUMs, both fixed before DONE with regression tests."
tasks_done:
  - id: CGHC-006
    tests: "84 controller"
    review: "code-reviewer PASS_WITH_FINDINGS (0 Crit/High); LOW-2 structural no-op install APPLIED"
    evidence: .loop-engineer/evidence/L6/CGHC-006-impl.md
  - id: CGHC-013
    tests: "156 service (15 session + 3 regression)"
    review: "code-reviewer PASS_WITH_FINDINGS (0 Crit/High); MEDIUM-1 post-terminal mutation leak + MEDIUM-2 reopen-clobbers-live-task both FIXED"
    evidence: .loop-engineer/evidence/L6/CGHC-013-impl.md
wave_3b2_totals:
  tasks_done: 2
  service_tests_passing: 156
  controller_tests_passing: 84
  unresolved_critical: 0
  unresolved_high: 0
tasks_done_total: 15
newly_ready:
  - CGHC-014
  - CGHC-016
  - CGHC-019
security_and_correctness_fixes_applied:
  - "CGHC-013 MEDIUM-1: task-registry.apply freezes on ANY terminal (not only cancel) + ev-reducer.foldOne drops post-terminal non-terminal frames (defense in depth) so no reduceEv consumer can append a post-terminal mutation; additive hardening of CGHC-012 (its tests stay green, acceptance unchanged, NOT invalidated)"
  - "CGHC-013 MEDIUM-2: session continueSession keeps a live non-terminal task instead of re-registering, so an in-flight StreamHandle is not orphaned and a cancelled task is not silently un-frozen"
  - "CGHC-006 LOW-2: NPM_PRESENT test helper injects a no-op install so 'no test runs real npm install' is structural, not incidental"
in_flight:
  - "Electron app scaffold (app/shell + app/ui + Vite + electron-builder + ADR) running as a sub-foundation; will be checkpointed separately when it lands + reviewed"
carried_forward_gating:
  - "CGHC-012: reducer now also enforces post-terminal drop (recorded as additive hardening, not a reopen)"
  - "CGHC-014: bindStream on stream start; feed EV via apply; return view on resync; supply real SessionStore + RuntimeHealth; preserve startSeq continuity"
  - "CGHC-016: add waiting_approval transition + denied terminal (reducer + freeze gate already accommodate)"
  - "CGHC-011 L4 still open: provider router testConnection handler must catch+scrub SocketPin/CrossHost/Ssrf errors"
resume_hint: >
  L6 Wave 3B-2 complete: 15/28 tasks DONE
  (CGHC-001/002/003/004/005/006/007/009/010/011/012/013/021/023/027). service suite 156 + controller 84
  green; 0 unresolved Critical/High. READY: CGHC-008 (workspace picker/UI), CGHC-014 (two-hop SSE),
  CGHC-016 (permission enforcement, CRITICAL), CGHC-019 (model switch), CGHC-020 (provider-error-map),
  CGHC-022 (settings UI), CGHC-024 (harness). Electron scaffold in flight (unblocks UI tasks). Next:
  land+review the scaffold, then build the secure DeepSeek credential-ingestion path + CGHC-024 offline
  harness -> CREDENTIAL GATE (one safe token-entry command, stop). Live tests (CGHC-024/028) authorized
  with DeepSeek behind OpenCode under the bounded constraints. L6 stays RUNNING until all 28 DONE and
  CGHC-028 release-verify PASSes. Web DEFERRED (ADR 0007).
