checkpoint: L6-cghc-017-done
created_at: "2026-07-11T19:40:00Z"
run_id: RUN-0007
loop_status: RUNNING
gate: null
resumable: true
tasks_done_total: 25
just_finished:
  - "CGHC-017 (permission UI) DONE — 25/28. Two layers: (A) token-guarded permission router (GET pending non-secret projection + POST decision -> gate.resolve) mounted in the composition root on the single PermissionGate; the route performs NO mutation, enforcement stays at gate.proceed. (B) UI modal + controller: P2 action+target, F5 description + honest hidden diff slot, fail-safe (ESC/backdrop never Allow), focus trap+restore, honest outcomes, token never in DOM, queue indicator."
reviews:
  - "Security co-sign PASS (0 Crit/High/Medium): could not break enforcement-at-the-boundary or deny-cannot-be-un-blocked; both test-proven incl. an on-disk deny (file never created). Token never in DOM, routes fail-closed token-guarded, body->400."
  - "UX/perf PASS (0 Crit/High): fail-safe + focus management correct and tested; a11y (dialog/aria/trap/restore, elevated not color-only, AA contrast light+dark). 5 LOW findings ALL FIXED."
low_fixes_applied:
  - "L1 (honesty): a resolved decision differing from the user's choice surfaces a truthful note (no fabricated success)."
  - "L2 (honesty/recovery): a failed decision POST preserves its error note when the still-pending modal re-opens (action stays blocked)."
  - "L3 (test): poll lifecycle start()/stop() proven via an injected timer seam (no real waits)."
  - "L4 (test): reverse Shift+Tab wrap + Enter/Space-on-Deny fail-safe."
  - "L5 (UX): truthful 'N waiting' queue indicator for >1 pending."
health:
  full_suite: "452 pass / 0 fail / 0 skip (stable over 2 runs)"
  tsc: "clean"
  permission_ui_tests: "28 pass"
  unresolved_critical: 0
  unresolved_high: 0
carry_forward:
  - "F5 full-diff content is a Tier-2 runtime enrichment (pending projection carries description/targetPath only; the modal slot is ready and never fabricates a diff)."
  - "CGHC-025 folds: session/visibilitychange-gated permission polling (currently a 2s loopback poll for the whole app lifetime) + aria-describedby on the dialog."
remaining:
  - "CGHC-025 (cold-start/renderer hardening, READY) — folds EV5 progress render + MEDIUM-5 rAF token-batching + permission-poll session-gating."
  - "CGHC-026 (extensions, deps met, MEDIUM/POC-budget)."
  - "CGHC-028 (packaged E2E release verification) — LAST; folds the Tier 2 live-supervisor legs."
resume_hint: >
  25/28 DONE, all reviewed 0 Crit/High, suite 452 pass, tsc clean. Composition root Tier 1 wired;
  permission UI Deny proven to block at the execution boundary end-to-end. Next: CGHC-025 (renderer
  hardening + the CGHC-015/017 UI carry-forwards), then CGHC-026, then CGHC-028 (packaged E2E + Tier 2
  live supervisor). Web DEFERRED (ADR 0007). No credential gate remains.
