checkpoint: L6-wave-3b
created_at: "2026-07-11T08:10:00Z"
run_id: RUN-0007
loop_status: RUNNING
gate: WAVE_PASS
resumable: true
note: "Wave 3 batch B. Parallelism capped at 2 concurrent per the stall-mitigation directive. CGHC-011 owner subagent stalled at the final file-size check (code+tests complete on disk); recovered by verify-on-disk. Both tasks got an independent security review (reviewer != owner); CGHC-023 came back CHANGES_REQUIRED and was fixed before DONE."
tasks_done:
  - id: CGHC-011
    tests: "138 service (14 provider-http-connector incl. F2/F3/M1)"
    review: "security-reviewer PASS_WITH_FINDINGS (0 Crit/High); MED-1 untested cross-host-non-resend CLOSED"
    evidence: .loop-engineer/evidence/L6/CGHC-011-impl.md
  - id: CGHC-023
    tests: "77 controller (22 clean incl. HIGH-1/MEDIUM-1 regressions)"
    review: "security-reviewer CHANGES_REQUIRED -> HIGH-1 (case-insensitive preserve bypass) + MEDIUM-1 (in-root junction) FIXED before DONE"
    evidence: .loop-engineer/evidence/L6/CGHC-023-impl.md
wave_3b_totals:
  tasks_done: 2
  service_tests_passing: 138
  controller_tests_passing: 77
  unresolved_critical: 0
  unresolved_high: 0
tasks_done_total: 13
newly_ready:
  - CGHC-006
  - CGHC-020
security_fixes_applied:
  - "CGHC-023 HIGH-1: overlap() folds case on win32 so a case-variant manifest entry (Docs/.GIT/CLAUDE.MD) can no longer delete a protected path on the case-insensitive target fs; 2 regression tests added"
  - "CGHC-023 MEDIUM-1: runClean reconciles each target's realpath against the preserve set, refusing an in-root junction/symlink that resolves onto a protected path"
  - "CGHC-011 MED-1: added the cross-host credential-non-resend test (CrossHostRedirectError, dialer dialed once) + bounded-hops + missing-Location cases"
carried_forward_gating:
  - "CGHC-011 L4: the provider router's testConnection handler MUST catch + scrub SocketPinViolationError / CrossHostRedirectError / SsrfBlockedError so no raw error/stack reaches the UI"
  - "CGHC-014 streaming: OpenCode re-resolves DNS at its own socket -> the streaming SSRF pin is a separate task (probe is the guarded call we control)"
  - "CGHC-006: real loopback service + graceful shutdown improves clean.bat running-detection via the same liveRecords seam"
resume_hint: >
  L6 Wave 3B complete: 13/28 tasks DONE
  (CGHC-001/002/003/004/005/007/009/010/011/012/021/023/027). service suite 138 + controller 77 green;
  0 unresolved Critical/High. READY now: CGHC-006 (lifecycle scripts), CGHC-008 (workspace picker/UI),
  CGHC-013 (session), CGHC-020 (provider-error-mapping), CGHC-022 (settings UI), CGHC-024 (harness).
  Next planned: CGHC-013 (session) + CGHC-006 (scripts), then the app/ Electron scaffold (unblocks
  CGHC-008/022/UI), then the secure DeepSeek credential-ingestion path + CGHC-024 offline harness ->
  CREDENTIAL GATE (one safe token-entry command, stop). Live tests (CGHC-024/028) authorized with
  DeepSeek behind OpenCode under the bounded constraints. L6 stays RUNNING until all 28 DONE and
  CGHC-028 release-verify PASSes. Web DEFERRED (ADR 0007).
