checkpoint: L6-wave-frontend-a
created_at: "2026-07-11T16:30:00Z"
run_id: RUN-0007
loop_status: RUNNING
gate: null
resumable: true
tasks_done_total: 23
numbered_tasks_done:
  - "CGHC-001..014, 016, 018, 019, 020, 021, 022, 023, 024, 027 (23/28)"
just_finished:
  - "CGHC-019 (model switch) DONE — code review PASS 0 Crit/High; LOW-2/3/4 fixed"
  - "CGHC-008 (workspace picker) DONE — code review PASS 0 Crit/High; MEDIUM 500->400 fixed with a reusable BadRequestError; LOWs fixed"
  - "CGHC-020 (provider error map) DONE — test-engineer + security co-sign PASS 0 Crit/High"
  - "CGHC-022 (settings store + clear-override) DONE — code review PASS 0 Crit/High; LOW-1 (audit clear-to-nothing) fixed"
health:
  full_suite: "372 pass / 0 fail / 0 skip"
  tsc: "clean"
  unresolved_critical: 0
  unresolved_high: 0
remaining:
  - "CGHC-015 (EV timeline UI + LIVE SSE dispatcher wiring + slow-consumer E2E) — unblocks 017/025"
  - "COMPOSITION ROOT (integration debt, prereq for CGHC-028)"
  - "CGHC-017 (permission UI, needs 015), CGHC-025 (cold-start hardening, needs 015), CGHC-026 (extensions)"
  - "CGHC-028 (packaged E2E release verification) — LAST"
integration_debt_composition_root:
  problem: "service/src/index.ts exposes createService + the mount seam + domain namespaces, but NOTHING assembles the routers into a running service; provider/session/permission/files/ev-stream are not even exported from the top barrel. Every wave used the staged 'orchestrator wires later' pattern."
  must_do_before_cghc_028:
    - "Mount ALL routers on one LocalService: health, workspace, credential, settings(diagnostics), provider, session, permission proxy, files, ev-stream (live SSE from CGHC-015)."
    - "Wire cross-cutting deps: PermissionGate <-> files ToolPermissionProxy <-> live RuntimeReplyPort (createLiveRuntimeReplyPort with a REDACTING error reporter, CGHC-016 LOW-2); ModelConfigService seeded from settings.defaultModel(); OpenCode runtime supervisor (buildLaunchSpec + credential injection from the OS store); EV two-hop stream bound to the session registry."
    - "setProviderBaseUrl must flow through provider-port.configureEndpoint (SSRF + DNS-rebinding re-check) — never persist an unvalidated base_url (CGHC-022 INFO-2)."
    - "Feed real credential values into the CGHC-021 SecretScrubber at boot so the value-based redaction layer is active (CGHC-022 INFO-2)."
    - "Add a loopback round-trip integration test (token guard -> route -> store/registry -> disk) proving SD1 + workspace grant + permission end-to-end (the unit layers are proven; the wired boundary is not)."
    - "Expose a composition-root start seam the shell/start.bat launches."
resume_hint: >
  23/28 DONE, all reviewed 0 Crit/High, suite 372 pass. Next: CGHC-015 (timeline UI + live SSE seam),
  then the COMPOSITION ROOT assembly (mount every router + wire cross-cutting deps + integration test),
  then CGHC-017/025/026, then CGHC-028 packaged E2E. No credential gate remains. Web DEFERRED (ADR 0007).
