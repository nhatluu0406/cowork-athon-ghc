checkpoint: L6-cghc-025-done
created_at: "2026-07-11T21:00:00Z"
run_id: RUN-0007
loop_status: RUNNING
gate: null
resumable: true
tasks_done_total: 26
just_finished:
  - "CGHC-025 (cold-start + renderer hardening) DONE — 26/28. Three Electron-scaffold GATES closed + progressive-readiness renderer surface + folded CGHC-015/017 carry-forwards."
gates_closed:
  - "GATE 1: renderer over a secure/standard app://cowork protocol; CSP as a REAL response header on every response (200/asset/403/404); path-traversal guard survives %2e%2e/double-encode/%5c/UNC/drive/sibling-prefix/NUL."
  - "GATE 2: boundary envelope lifted into @cowork-ghc/contracts (single source of truth); renderer deleted its drifted copy + RE-INSTATED the protocol-version rejection (client + EV snapshot unwrap)."
  - "GATE 3: real webPreferences (sandbox/contextIsolation true, nodeIntegration false), preload bridge = exactly getBootstrap+pickWorkspaceFolder (no ipcRenderer/require), 4 navigation denials look-alike-origin-proof; 23 app/shell security-baseline tests (were 0)."
progressive_readiness: "honest starting->connecting->ready (ready only on a real health() success); unreachable/not_connected with Retry + scrubbed diagnostics; bounded backoff; never fake-ready; no token/base-url in DOM."
folded_carry_forwards:
  - "EV5 progress render (SessionView.progress + role=progressbar; terminal clears it)."
  - "MEDIUM-5: append-delta text + rAF-aligned coalescer (no O(N^2) re-serialize)."
  - "permission-poll visibility gating + aria-describedby."
  - "service-client.ts split (<250) into service-client + permission-client (public surface unchanged)."
review: "Independent security review (security-reviewer != implementer) PASS, 0 Critical/High. All 4 highest-value targets held (CSP-real-header, path-traversal, renderer isolation, no-secret-in-DOM). MEDIUM (protocol-check test gap) + 2 LOW (snapshot protocol guard, scrub 2 error paths) ALL FIXED + re-verified."
health:
  full_suite: "500 pass / 0 fail / 0 skip (stable over 2 runs)"
  tsc: "clean"
  app_ui_tests: "64 pass"
  app_shell_tests: "23 pass"
  unresolved_critical: 0
  unresolved_high: 0
carry_forward_cghc_028:
  - "LIVE multi-process supervisor: spawn the loopback service + OpenCode child, real crash detection + restart (this task built the honest renderer surface, not the process supervisor)."
  - "Live-Electron E2E: BrowserWindow actually receives buildMainWindowWebPreferences; app:// is the real loader; CSP header on a live renderer document; sandbox denies require at runtime; live nav/window.open/webview blocked."
  - "Symlink-realpath re-check in app-protocol.ts + no-symlinks-in-ui/dist packaging guard; sandboxed-preload single-bundle packaging."
remaining:
  - "CGHC-026 (runtime extensions, MEDIUM/POC) — agent in flight; seam-based skill/MCP/template + RE5 isolation; live MCP/skill execution deferred to Tier 2."
  - "CGHC-028 (packaged E2E release verification) — LAST; folds every Tier 2 live leg (supervisor, live Electron, live MCP, packaging guards)."
resume_hint: >
  26/28 DONE, all reviewed 0 Crit/High, suite 500 pass, tsc clean. Composition root Tier 1 wired; the app
  hardens + boots honestly. CGHC-026 (extensions) is in flight; after it only CGHC-028 (packaged E2E +
  the consolidated Tier 2 live-supervisor/live-Electron/live-MCP legs) remains. Web DEFERRED (ADR 0007).
  No credential gate remains.
